;; ============================================================
;; src/main.watx — THE source manifest for Wine-Assembly.
;;
;; This file is the ONE hand-maintained source order, and there is no longer a
;; copy of it anywhere. Everything that needs the order DERIVES it from here:
;;
;;   - lib/wat-manifest.js — parses the include list below. Every Node consumer
;;     reads it from there, including the WAT_FILES that lib/compile-wat.js
;;     still re-exports (a getter now, not an array of its own).
;;   - lib/watx-launcher.js — the browser fetches this file and parses the same
;;     list, so the page's source build uses no manifest but this one.
;;   - tools/concat-wat.js — writes build/combined.wat from that list, adding
;;     the outer (module ...) wrapper that no source fragment carries.
;;
;; TO ADD OR REORDER A PART: edit the include list HERE. That is the whole
;; procedure. Filename order (LC_ALL=C sort) is enforced by
;; tools/check-wat-manifest.js, which also catches the one thing the compiler
;; cannot: a src/*.wat that no (include ...) below names is not part of the
;; program, and nothing would otherwise say so.
;;
;; Include paths are relative to src/, so a driver populates the compiler's vfs
;; by reading src/<name> for each key.
;;
;; No fragment below contains a (module ...) wrapper: each balances its own
;; parentheses (gated by tools/check-wat-fragments.js), which is what lets the
;; WATX compiler's include resolver splice them as top-level module fields and
;; what lets lib/compile-wat.js parse each part independently.
;; ============================================================
(include "00-regions.wat")
(include "01-header.wat")
(include "01b-api-hashes.generated.wat")
(include "02-thread-table.wat")
(include "03-registers.wat")
(include "04-cache.wat")
(include "05-alu.wat")
(include "05b-string-ops.wat")
(include "05c-seg16-ops.wat")
(include "06-fpu.wat")
(include "06b-core-handlers.wat")
(include "06c-mmx.wat")
(include "07-decoder.wat")
(include "07b-loop-match.wat")
(include "07c-block-exec.wat")
(include "08-pe-loader.wat")
(include "08b-dll-loader.wat")
(include "08c-ne-loader.wat")
(include "09a-handlers.wat")
(include "09a0-handlers-base.wat")
(include "09a0b-handlers-base-late.wat")
(include "09a0c-handlers-device-io.wat")
(include "09a1-comctl-handlers.wat")
(include "09a2-handlers-console.wat")
(include "09a3-handlers-audio.wat")
(include "09a4-handlers-gdi.wat")
(include "09a4b-handlers-cursor-icon.wat")
(include "09a5-handlers-window.wat")
(include "09a5b-handlers-window-late.wat")
(include "09a5c-handlers-wide.wat")
(include "09a5d-handlers-windowpos.wat")
(include "09a6-handlers-crt.wat")
(include "09a7-handlers-dispatch.wat")
(include "09a7b-ole.wat")
(include "09a7c-mixer.wat")
(include "09a7d-handlers-shell-file.wat")
(include "09a8-handlers-directx.wat")
(include "09a8b-handlers-opengl.wat")
(include "09a8c-gl-encoder.wat")
(include "09a8d-gl-abi.generated.wat")
(include "09a8e-gl-state.wat")
(include "09a9-comctl32.wat")
(include "09aa-handlers-d3dim.wat")
(include "09ab-handlers-d3dim-core.wat")
(include "09ac-handlers-d3d8.wat")
(include "09ad-handlers-d3d9.wat")
(include "09ae-d3d9-resources.wat")
(include "09af-d3d-shader-ir.wat")
(include "09ag-d3d-shader-vm.wat")
(include "09ah-d3d-software.wat")
(include "09ai-d3d-render-lifetime.wat")
(include "09aj-d3d-fixed.wat")
(include "09ak-d3d-depth.wat")
(include "09al-d3d-reset.wat")
(include "09am-d3d-color.wat")
(include "09b-dispatch.wat")
(include "09b2-dispatch-table.generated.wat")
(include "09c-help.wat")
(include "09c0-window-table.wat")
(include "09c2-treeview.wat")
(include "09c3-controls.wat")
(include "09c3-controls0-basic-wndprocs.wat")
(include "09c3-controls1-browse-dialog.wat")
(include "09c3-controls2-open-save-dialog.wat")
(include "09c3-controls3-color-dialog.wat")
(include "09c3-controls4-shell-dialogs.wat")
(include "09c3-wndprocs.wat")
(include "09c3-wndprocs1-toolbar.wat")
(include "09c3-wndprocs2-tooltip-trackbar.wat")
(include "09c3-wndprocs3-listbox.wat")
(include "09c3-wndprocs4-combobox.wat")
(include "09c3-wndprocs5-edit.wat")
(include "09c3a-dialog-runtime.wat")
(include "09c3b-scrollbar.wat")
(include "09c4-defwndproc.wat")
(include "09c5-menu.wat")
(include "09c6-winhelp-core.wat")
(include "09c7-winhelp-hlp.wat")
(include "09c8-winhelp-cnt.wat")
(include "09c9-winhelp-ui.wat")
(include "09d-winsock.wat")
(include "09d1-mpr.wat")
(include "09d2-tapi.wat")
(include "09d3-spooler.wat")
(include "09d4-dplay-net.wat")
(include "09e-win16-api.wat")
(include "09e2-win16-dialog.wat")
(include "09f-win16-ddeml.wat")
(include "10-helpers.wat")
(include "10a-gdi-bitmap.wat")
(include "10b-gdi-font.wat")
(include "10c-truetype.wat")
(include "10c1-truetype-hint.wat")
(include "10d-gdi-region-path.wat")
(include "10e-gdi-metafile.wat")
(include "10f-gdi-dc.wat")
(include "10g-gdi-raster.wat")
(include "11-seh.wat")
(include "12-wsprintf.wat")
(include "13-exports.wat")
